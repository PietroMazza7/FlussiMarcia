# FlussiMarcia
Il sito ha lo scopo di contare il numero di partecipanti a una marcia e di prevedere il loro spostamento approssimativo.

Dopo aver installato con `pip install -r requirements.txt` usre il comando `python3 run.py`